import { Button, VerticalBox } from "std-widgets.slint";

//export global Logic {
//    pure callback button-clicked();
    // You can collect other global properties here
//}

export component SingleButton inherits Window {
    callback button-clicked(string) -> bool;
    b1 := Button {
        text: "OK";
        clicked => {root.button-clicked(b1.text);}
    }
}

